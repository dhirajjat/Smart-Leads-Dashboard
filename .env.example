# Express Server Secrets
JWT_SECRET="smart-leads-dashboard-secret-key-replace-in-production"
PORT=3000
NODE_ENV="development"

# MongoDB URI (Optional: If not provided, the app uses a robust file-based storage for the demo)
MONGO_URI="mongodb+srv://dheerajjat9098_db_user:vFm8f2SEl5XP4vgL@cluster0.k4v2cot.mongodb.net/?appName=Cluster0"

# Frontend settings (Exposed to client in Vite)
# Set this to your backend host (e.g. https://your-server.onrender.com) when deploying frontend separately to Vercel
VITE_API_BASE_URL=""
